# Build tools resolved together with pyproject.toml's webapp extra.
# Regeneration command and supported target are documented in desktop/README.md.
pip
setuptools>=80
setuptools-scm>=9.2
wheel
build>=1.2
pyinstaller==6.22.2
