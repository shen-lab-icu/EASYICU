"""Typed runtime and validation contracts for the research agent.

Keep this package initializer lazy: callers import the small owner module they
need instead of loading every contract whenever Python resolves the package.
"""
