"""Deterministic executors for science already fixed by the Agent.

These modules render or compute a closed, planner-owned contract.  They do not
select the cohort, exposure, outcome, method, covariates, or estimand.
"""
