## Scope

- Owner module and public contract:
- Starting base HEAD (full SHA):
- Explicitly out of scope:

- [ ] This PR contains one independently reviewable workstream.
- [ ] Work was performed in a dedicated linked worktree, or a sole-user clone
      exception is explained below.
- [ ] No unrelated staged, unstaged, or untracked path was present at commit.

## Task-scope receipt

Run `tools/verify_git_task_scope.py` immediately before committing.

```text
scope_sha256:
allowed_paths:
staged_paths:
unexpected_paths: []
```

## Verification

- Focused negative/contract tests and result:
- Full repository CI: not run / exact command and result
- Exact PR head (full SHA) verified:

- [ ] Focused checks are not described as full-repository CI.
- [ ] Required CI is green for the exact PR head before merge.

## Independent review

- Required reviewer/domain:
- Reviewer requested:
- Approval recorded:

- [ ] An independent reviewer (not the author) has approved the owning domain
      before merge, or the documented rationale below establishes why no domain
      review is required.

Exception or no-review rationale:
