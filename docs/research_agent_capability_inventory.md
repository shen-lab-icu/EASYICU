# Research-agent capability inventory

**What this is.** The reachability registry for package-local modules that look
unreachable to the static import graph. It distinguishes code that a public
entry point demonstrably reaches from experimental or disabled code. "Already
written" is not the same as "available in the product". Each production row
must name the exact public-API-to-executor route and one integration test that
traverses it; every other row must stay visibly experimental or disabled.

**What this is not.** It is not a redundancy audit. Zero-inbound-import is only
the *most visible* kind of unused code; it says nothing about unreachable
branches inside wired modules, string- or template-dispatched entry points
(`run_feasibility_protocol` is called from a generated code template and looks
orphaned to any import scan), or paths that exist but are never traversed in a
real run. Do not compute "% of the module that is live" from this file.

**How to regenerate the candidate list**

```bash
python tools/research_agent_module_graph.py > /tmp/graph.json
```

Modules with in-degree 0 that are not `__init__.py` are candidates; then check
each leaf name against `src/easyicu` (excluding `research_agent`), `tests/`,
`tools/` and `pyproject.toml` before calling anything unused. Console-script
entry points (`cli`, `replication_cli`) are reached through
`[project.scripts]`, not imports.

Measured 2026-08-14 at `26bdcdc`.

## Status vocabulary

| status | meaning |
| --- | --- |
| `production_reachable` | A public API reaches the real runtime owner, and the row binds an exact integration test proving that route. |
| `experimental` | Implemented or tested, but not advertised as a stable product capability and not relied on by the default runtime. |
| `disabled` | Intentionally unavailable on production paths; retained only for a named compatibility/removal boundary. |

## Inventory

| module | LOC | status | owner | activation precondition | tests | route proof | review |
| --- | ---: | --- | --- | --- | --- | --- | --- |
| `methods/rmst.py` | 264 | `experimental` | methods | `time_to_event.rmst` is published before planning. A host-owned deterministic executor (`execution/runners/rmst_executor.py`, selection key `signed_rmst_contrast`) claims only a step bound to one reviewed two-group RMST specification and the typed analysis cohort, with a typed runtime receipt. It remains analysis-only: no external survRM2 oracle and no reportable promotion exist yet. | 21 | - | 2026-10-01 |
| `methods/decision_curve.py` | 228 | `experimental` | methods | `prediction.decision_curve` now reaches Planner → typed Plan action → Coder resource selection. It remains Coder-generated/analysis-only until a deterministic DCA owner, panel and external oracle are registered. | 11 | - | 2026-10-01 |
| `methods/delong_auc.py` | 285 | `experimental` | methods | `prediction.delong_ci` now reaches Planner → typed Plan action → exact Coder kernel and remains oracle-checked against `pROC::roc.test`; no deterministic host owner currently promotes it. | 4 | - | 2026-10-01 |
| `methods/conformal.py` | 165 | `experimental` | methods | `prediction.conformal_intervals` now reaches Planner → typed Plan action → exact Coder kernel. It remains exploratory/analysis-only until a typed split/coverage contract and deterministic validator are registered. | 5 | - | 2026-10-01 |
| `methods/survival_inputs.py` | 219 | `experimental` | methods | Published to Planner as a reviewed survival supporting primitive and available to the digest-bound Coder selector; it is not a standalone estimand owner and no production executor currently consumes it directly. | 4 | - | 2026-10-01 |
| `methods/temporal_features.py` | 306 | `experimental` | methods | Published to Planner as a reviewed survival/causal timing primitive and available to the digest-bound Coder selector; the trajectory/landmark scientific contract still governs whether it can support a reportable result. | 9 | - | 2026-10-01 |
| `method_skills.py` | 1127 | `production_reachable` | method-skills | Public `GET /api/capabilities` → `webserver.capabilities.capability_status` → `method_skill_catalog` → reviewed workflow/component catalogue, including the bounded early-subtype package. | `tests/webserver/test_webserver_capabilities.py::test_method_skill_catalog_projects_registered_method_contracts` | `call:get;trace:phenotyping.early_subtype_assignment` | 2026-11-01 |
| `methods/subtype_assignment.py` | 308 | `experimental` | methods | Leakage-bounded early trajectory-subtype assignment kernel used by the generated method-skill package. It remains analysis-only until a public research-agent execution route binds its patient-disjoint split and evidence contract. | 2 | - | 2026-11-01 |
| `methods/dynamic_prediction.py` | 313 | `experimental` | methods | `dynamic_prediction` → required typed action `prediction.dynamic_prediction` → digest-bound Coder resources (`dynamic_prediction`, `temporal_features`, `sklearn`). The reviewed kernel owns leakage-safe landmark feature/label/evaluation helpers. A separate deterministic binary fit owner now exists experimentally, but it is not registered on this landmark Planner/runtime route, so fitting here remains generated and analysis-only. | `tests/research_agent/core/test_scientific_action_catalog.py::test_dynamic_action_selects_sklearn_and_both_reviewed_timing_kernels` | - | 2026-10-01 |
| `execution/runners/prediction_validation_executor.py` | 22 | `experimental` | prediction-validation | Compatibility-only incubator adapter re-exporting the dependency-neutral prediction-validation owner. It remains outside Planner/runtime selection and contains no EvidenceStore or paper-authority logic. | 38 | - | 2026-10-01 |
| `prediction_model_fit_owner.py` | 1100 | `experimental` | prediction-model-fit | Direct experimental API consumes one host-issued immutable typed input plus a fully fixed subject-level declaration. Numeric median imputation, standardization and L2 logistic regression are fitted only on declared training subjects; exact model JSON, all-row prediction CSV, source/split/model byte digests, package versions and an immutable analysis-only receipt are issued together. Test-only extremes cannot alter train-fitted state, the model artifact independently reconstructs probabilities, and full recomputation rejects payload/model/receipt/source/contract drift. The same owner now serializes the exact consumed-column projection and exposes a computation-only persisted-artifact re-fit so the authority layer does not copy sklearn logic; this lower route grants no evidence or claim authority. | 23 | - | 2026-10-01 |
| `prediction_validation_owner.py` | 663 | `experimental` | prediction-validation | Shared host owner for the execution adapter and EvidenceStore bridge. It consumes the typed declaration and exact digest-bound UTF-8 CSV bytes, performs the deterministic full receipt recomputation, reconciles the cohort subject set and one subject-disjoint split assignment against every prediction subject, and issues the runtime-bound zero-finding seal internally. Stable cohort/split mismatch reasons cover missing columns, missing or extra subjects, duplicate assignments and split disagreement. Frozen and live base-R oracles still cover AUROC, Brier score, non-identity joint calibration coefficients and threshold counts. It trains no model and grants no claim authority. | 56 | - | 2026-10-01 |
| `authority/prediction_validation_evidence.py` | 550 | `experimental` | prediction-validation | Direct incubator bridge resolves seven exact, digest-verified upstream EvidenceStore records (prediction table, cohort, subject-disjoint split, model, source snapshot, environment lock and runtime receipt), then invokes the shared owner against current stored bytes. The public registration route accepts no caller-supplied receipt or host seal, requires exact cohort membership and split semantics, and registers one deterministic `analysis_only` bundle. Its exact artifact and runtime-subset resolvers are reused by the fit-evidence owners so current-byte and runtime policy is not copied. Registration publishes no aliases, numeric claims or scientific claims; revalidation repeats host recomputation and detects later authority promotion. It remains outside Planner/runtime selection and grants no paper authority. | 18 | - | 2026-10-01 |
| `authority/prediction_model_fit_evidence.py` | 547 | `experimental` | prediction-model-fit | Host-owned composite bridge accepts one V4 sealed fit, the same immutable typed input, a matching validation contract and three exact runtime-authority records. It revalidates the complete fit before writing, materializes canonical source/split/prediction roles plus a model envelope carrying the fit spec, fit receipt, typed-input receipt and model state, builds the closed seven-role lineage internally, then delegates deterministic metrics and analysis-only registration to V3.1. The public route accepts no loose lineage, model, prediction, receipt or seal; retries are idempotent, all four fit-role byte drifts fail current-store validation, and no aliases or claims are published. V5.1 adds a separate host runtime-capture route but does not change this bridge's Planner or paper ceiling. | 10 | - | 2026-10-01 |
| `authority/prediction_model_fit_runtime.py` | 491 | `experimental` | prediction-model-fit | Host runtime owner accepts no repository path, caller runtime identity, artifact payload or digest. It requires the module's actual Git checkout to be tracked/untracked clean, captures exact commit and Git tree, reconciles installed fit packages against the sealed V4 receipt, and registers canonical code/environment/runtime records without aliases or claims. Revalidation re-observes the current checkout and environment and refuses commit, tree, dirty-state, package, interpreter or stored-record drift. Local-process only; it grants no container, Planner, production or paper authority. | 7 | - | 2026-10-01 |
| `authority/prediction_model_fit_revalidation.py` | 273 | `experimental` | prediction-model-fit | Persisted V5.1 validator accepts only EvidenceStore plus the composite registration. It resolves all seven current records, revalidates the canonical model envelope, re-observes the clean runtime, re-fits the current persisted source projection through the sole fit owner, compares model/prediction/receipt bytes, then repeats V3.1 metric/current-store validation. It writes nothing and publishes no alias or claim. This closes persisted re-fit for the fixed synthetic v1 estimator but is not an external oracle, real-data validation, Planner route or paper authority. | 5 | - | 2026-10-01 |
| `evaluation_scorecard.py` | 1628 | `experimental` | evaluation | Optional Tier-2 scoring over completed artifacts. Paper-facing scorer authority lives outside the installed engine, under `benchmarks/`. | 7 | - | 2026-11-01 |
| `evaluation/tier2_jury.py` | 649 | `experimental` | evaluation | Optional jury/rubric adapter; not enabled on the default production path. | 8 | - | 2026-11-01 |
| `skill_packages/landmark_categorical_association/*` | 3196 | `experimental` | skill-packages | Executable reference workflow (Biomni-style skill package) for the fixed-landmark categorical association family. Reachable today through the Skill Hub package projection (`method_skills.REFERENCE_SCRIPT_PACKAGES` → `GET /api/capabilities/method-skills/fixed-landmark-association-study/package` shows its `scripts/`), direct import of `load_cohort` → `run_analysis` → `generate_all_plots` → `export_all`, and the module CLI (`python -m easyicu.research_agent.skill_packages.landmark_categorical_association`, zero-inbound `__main__.py`). It composes the host kernels (`landmark_eligibility_mask`, `run_adjusted_association_from_env`, `fit_estimator`, ordered-trend and RCS primitives, grouped Table 1) and never selects exposure, outcome, cohort or estimand. It is not yet selected by the pipeline's step-executor registry, so its outputs stay analysis-only until a typed runner route binds it. | 15 | - | 2026-11-01 |
| `planning/family_spec/*` | 6354 | `experimental` | planning | `planner_strategy=family_spec_v1` (PipelineConfig; pinned by the 20260922 E1 dev profiles that the Web Copilot selects). For a sealed context whose headline analysis type fits a host template (`family_template_id_for_context`): fixed-landmark categorical association (E3/M1), fixed-landmark continuous exposure with the signed spline runtime (E2), descriptive exposure–outcome with the counts-only distribution owner (E1), cross-sectional phenotyping over window-bound features with an optional membership predicate (M3), static binary prediction with the host static-prediction owner (M2), and three sealed host suites that the pipeline discloses through `ScientificRuntimeAuthorities.planning_contract_context()`: the fixed-landmark survival suite (H1; `LandmarkSurvivalRuntimeAuthority`), the fixed-window trajectory suite (H3; `TrajectoryScientificRuntimeAuthority`) and the fail-closed source-feasibility decision (H2; `SourceFeasibilityRuntimeAuthority`, whose `formal_result_scope` narrows the article, figure and robustness contracts to that one result and forbids a primary effect step), where the Planner only labels the sealed columns and writes comparator applications and the host's `bind_plan` replaces the draft with the signed owners (`tests/research_agent/planning/test_family_spec_sealed_suites.py`). The Progressive agent makes one Provider call for a `FamilyPlanSpec` (adjustment roster with rationales, baseline/feature rosters, membership column, reader labels, comparator applications — each family's spec fields are validated by `validate_family_plan_spec`), the host template projects outline/foundation/every step, and the unchanged Progressive v2 outline authority, foundation validator, prefix compiler and final acceptance judge it (`ProgressivePlannerAgent._run_family_spec_output` → `run_family_spec_attempt`). Any other family (for example a causal contrast with no sealed feasibility decision, or survival/trajectory without a sealed authority), a development resume, or an outline-only canary records `family_spec_fallback_reason` and runs the exact Progressive v2 path. Planner-selected covariates carry only host-proven timing (`adjustment_authority.host_window_bound_roles`); an exact user roster stays user authority; templates never select exposures, outcomes, or sensitivity axes. End to end in the Web: a `planner_selectable` landmark study compiles a v3 `LandmarkCategoricalAssociationRuntimeAuthority` / v5 `LandmarkSplineRuntimeAuthority` whose `plan_bound_adjustment_roster` seals the executable column domain, and `ScientificRuntimeAuthorities.seal_for_plan` re-signs it with the reviewed primary model's roster before validation and execution (`tests/research_agent/authority/test_landmark_categorical_plan_bound_roster.py`, `tests/research_agent/planning/test_family_spec_planner.py`). | 24 | - | 2026-11-01 |
| `agents/family_spec_planner.py` | 694 | `experimental` | agents | Provider-facing half of the family-spec strategy: the spec prompt, run-bound strict JSON schema (covariate names, label keys and citation keys are closed enums from the sealed request), the fail-closed parser, and `run_family_spec_attempt`, which the Progressive agent calls with its own outline binders and acceptance callables so no plan gate is bypassed. Reached only through `planner_strategy=family_spec_v1`. | 18 | - | 2026-11-01 |
| `evaluation/cross_model_panel.py` | 294 | `experimental` | evaluation | Optional cross-model concordance; not enabled on the default production path. | 5 | - | 2026-11-01 |
| `acquisition/foundation.py` | 603 | `experimental` | acquisition | The opt-in Web canary route calls this owner before `ResearchAgentPipeline.run`, but the current integration test replaces both acquisition and Pipeline with fakes. Keep the route experimental until a bounded canary or non-fake integration traverses the real acquisition-to-pipeline boundary. | `tests/webserver/copilot/test_pi_copilot_research_workflow.py::test_web_runner_delegates_to_research_agent_pipeline` | - | 2026-11-01 |
| `acquisition/hospital_followup_materialization.py` | 100 | `experimental` | acquisition | Typed materializer for hospital-discharge follow-up authority. It is covered at its owner boundary but remains outside a stable default Planner-to-runtime route and grants no publication authority. | - | - | 2026-11-01 |
| `acquisition/time_varying_materialization.py` | 250 | `experimental` | acquisition | Typed materializer for governed time-varying analysis inputs. It remains an E1/E2 integration capability until a public route traverses the real source-to-executor boundary with sealed evidence. | - | - | 2026-11-01 |
| `intake/external_benchmark_authority.py` | 203 | `experimental` | benchmark-intake | The external benchmark CLI imports this owner dynamically to validate explicit endpoint, covariate, concept, time-window and column-role declarations before Provider launch. It remains benchmark/development-only and grants neither default product reachability nor paper authority. | `tests/research_agent/authority/test_external_benchmark_authority.py::test_external_study_authority_fails_closed_before_provider` | - | 2026-10-01 |
| `discovery/idea_mining_source_status.py` | 1144 | `experimental` | idea-mining | Idea-mining lane. Needs the differentiated fresh Idea end-to-end run before this is advertised as a stable product capability. | 1 | - | 2026-10-01 |
| `discovery/idea_mining_extended_feasibility.py` | 680 | `experimental` | idea-mining | Same lane. | 5 | - | 2026-10-01 |
| `discovery/idea_mining_construct_answerability.py` | 637 | `experimental` | idea-mining | Computes bounded construct-answerability rows for a source-bound candidate. It remains a review gate within the Idea Mining lane, not proof that a candidate is executable or publishable. | - | - | 2026-11-01 |
| `discovery/idea_mining_data_first_route.py` | 627 | `experimental` | idea-mining | Same lane. | 3 | - | 2026-10-01 |
| `discovery/concept_proposal.py` | 460 | `experimental` | idea-mining | Same lane. | 5 | - | 2026-10-01 |
| `discovery/idea_mining_longitudinal.py` | 349 | `experimental` | idea-mining | Same lane. | 2 | - | 2026-10-01 |
| `discovery/longitudinal_handoff.py` | 263 | `experimental` | idea-mining | Same lane. | 1 | - | 2026-10-01 |
| `authority/source_status_sdk.py` | 240 | `experimental` | authority | Public SDK candidate for source-status receipts; no production consumer is currently registered. | 1 | - | 2026-10-01 |
| `execution/native_method_runtime.py` | 85 | `experimental` | execution | Dependency-neutral dispatch boundary for reviewed native methods. It is not yet a stable public execution route and cannot promote analysis-only results or claims. | - | - | 2026-11-01 |
| `reporting/system_validation_report.py` | 782 | `experimental` | reporting | The governed reviewer demo's completed, privacy-passing Web projection calls this engineering-only report builder, renderer and receipt owner. The exact integration test starts at private `_write_projection`, so no public production route is claimed. | `tests/webserver/copilot/test_pi_copilot_research_workflow.py::test_pipeline_projection_uses_real_artifacts_and_withholds_identifier_table` | - | 2026-11-01 |
| `planning/step_phase.py` | 168 | `experimental` | planning | Case-neutral compiled phase projection used by the Web plan reader. It does not alter sealed AnalysisStep bytes or grant execution or claim authority; keep it experimental until a public research-agent route consumes the compiled phase. | 18 | - | 2026-11-01 |
| `planning/requirement_coverage.py` | 159 | `experimental` | planning | Typed digest-bound projection drafted for explicit outcome, sensitivity, and covariate coverage. No current Planner or runtime owner imports it, so it grants no execution, evidence, or claim authority until a reviewed integration and owner test are added. | - | - | 2026-11-01 |
| `reporting/article_display_package.py` | 377 | `experimental` | reporting | Submission-package inspection owner used by the manuscript-reader build tool and focused tests. It reports planning signals only and is not part of the default runtime or a scientific acceptance gate. | 17 | - | 2026-11-01 |
| `reporting/article_display_policy.py` | 35 | `experimental` | reporting | Compatibility-only re-export of the figure-owned display policy. The implementation lives in `figures/contracts.py`; this path preserves callers during the ownership migration and grants no separate authority. | 16 | - | 2026-11-01 |
| `reporting/article_population_alignment.py` | 72 | `experimental` | reporting | Typed article-population alignment receipt covered by focused tests but not selected by the default Writer/runtime route. It grants no claim authority until a public integration consumes it. | 7 | - | 2026-11-01 |
| `reporting/writer_only_migration.py` | 969 | `experimental` | reporting | Explicit repair-tool owner for migrating an existing sealed manuscript without mutating the source run. It is reachable only through the standalone repair tool, not the default pipeline, and remains analysis-only. | 15 | - | 2026-11-01 |
| `contracts/rmst.py` | 41 | `experimental` | contracts | Frozen finite RMST coordinate contract only; not yet an AnalysisStep field or selected executor. Plan binding, adapter execution and end-to-end verification remain pending. | 12 | - | 2026-11-01 |
| `contracts/trajectory_design.py` | 541 | `experimental` | contracts | The reviewable half of a fixed-window trajectory study (coordinates, window and grid, candidate cluster grid, stability design) plus the host policy that has exactly one implementation (model family, fit engine, BIC rule, seeds, tolerances, boundary reason codes). Reached from outside the package: `webserver/study_contexts.normalize_trajectory_design` validates the StudyContext field `trajectory_design` on write, and `webserver/trajectory_runtime_projection` compiles it into a sealed `TrajectoryScientificRuntimeAuthority` that the Web launch path puts in `PipelineConfig.trajectory_scientific_runtime_authority`. It stays experimental for the same reason as `acquisition/foundation.py`: the Web integration test replaces acquisition and Pipeline with fakes, so no non-fake route traverses StudyContext to executor yet. | `tests/webserver/test_web_trajectory_runtime_projection.py` | - | 2026-11-01 |
| `reporting/revision_figures.py` | 291 | `experimental` | reporting | Reached only through the Web report-revision repair and export tools (`webserver/manuscript_repair.py`, `webserver/report_revision_export.py`): it builds and verifies the revision figure bundle for an existing sealed run. It is not selected by the default pipeline and grants no execution, evidence or paper authority until a public route traverses the real revision-figure boundary. | 3 | - | 2026-11-01 |
| `reporting/result_card.py` | 281 | `production_reachable` | reporting | Public Pi `easyicu_inspect_interpretation` tool → `build_result_interpretation_card` → bounded aggregate-only projection. | `tests/webserver/copilot/test_pi_copilot_research_workflow.py::test_interpretation_and_manuscript_tools_bound_large_agent_drafts` | `call:tool_module.execute_tool;trace:easyicu_result_interpretation_projected` | 2026-11-01 |
| `replication/discovery.py` | 698 | `production_reachable` | replication | Public `discover_easyicu_exports` → native/legacy ExportPackage discovery → verified package result. | `tests/research_agent/core/test_export_package_integration.py::test_replication_discovery_accepts_native_and_legacy_packages` | `call:discover_easyicu_exports` | 2026-11-01 |
| `cli.py` | 426 | `production_reachable` | agent | `[project.scripts] easyicu-research-agent` → `cli.main` → `ResearchAgentPipeline.run`. | `tests/research_agent/core/test_cli.py::test_public_cli_reaches_the_research_agent_pipeline` | `call:cli.main;trace:run` | 2026-11-01 |
| `script_runtime.py` | 106 | `production_reachable` | agent | Public `script_runtime.write_json` → strict JSON artifact with recursive non-finite normalization. | `tests/research_agent/core/test_script_io_replaces_the_pasted_helper.py::test_the_imported_writer_emits_null_for_every_non_finite_number` | `call:write_json` | 2026-11-01 |
| `graph.py` | 55 | `disabled` | orchestration | Retired LangGraph builder fails closed; removal target is 2.0 in `docs/deprecation_policy.md`. | 1 | - | 2.0 |
| `plan_utils.py` | 81 | `disabled` | planning-compatibility | Compatibility-only re-export shell retained for existing imports; it has no production route and is a removal target for 2.0. | 3 | - | 2.0 |
| `scientific_adapters/*` | 52 | `experimental` | scientific-adapters | Optional dowhy / pandera / sksurv adapters are dynamically probed but may not upgrade current scientific capabilities. | 10 | - | 2026-11-01 |
| `replication_cli.py` | 228 | `production_reachable` | replication | `[project.scripts] easyicu-research-replication` → `replication_cli.main` → `ResearchAgentPipeline.reproduce_paper`. | `tests/research_agent/core/test_replication_cli.py::test_replication_cli_paper_mode_dispatches_to_pipeline` | `call:replication_cli.main;trace:reproduce_paper` | 2026-11-01 |
| `case_plugins/` | 26 | `experimental` | case-plugins | Plugin discovery surface; default production registry remains empty until an explicit plugin activation contract is supplied. | 7 | - | 2026-11-01 |
| `facade.py` | 100 | `production_reachable` | agent | Public `easyicu.research_agent.facade.go` → `ResearchAgentPipeline.run` with fail-closed offline mock; replicate/resume wrappers delegate to the same pipeline. | `tests/research_agent/core/test_facade.py::test_go_defaults_to_offline_mock_and_delegates_to_pipeline` | `call:facade.go;trace:ResearchAgentPipeline.run` | 2026-11-01 |
| `benchmark_instances.py` | 60 | `production_reachable` | agent | Public `easyicu.research_agent.benchmark_instances.evaluate` → `icu_agent_bench.grade_bench_task` over the frozen checkable subset. | `tests/research_agent/core/test_benchmark_instances.py::test_evaluate_scores_a_frozen_instance_in_one_call` | `call:evaluate;trace:grade_bench_task` | 2026-11-01 |
| `authority/tool_promotion.py` | 301 | `experimental` | authority | Promotion gate for Tool Cards: single runs classify as `sandbox_code`/`candidate` only; `verified_tool` requires a complete card plus qualifying independent reproductions and an applicability attestation. No Planner or runtime owner calls it yet, so it grants no execution, evidence, or claim authority until a reviewed integration wires it. | `tests/research_agent/authority/test_tool_promotion_chain.py` | - | 2026-11-01 |
| `gates/literature_retrieval_gate.py` | 284 | `experimental` | gates | Read-only deterministic preflight: plan decisions (population, outcome/window, method applicability) must bind bibliographic retrieval evidence or the gate reports findings. It spends no Provider budget and mutates no authority; no Planner or runtime owner consumes its verdict yet. | `tests/research_agent/test_literature_gate_retrieval.py` | - | 2026-11-01 |
| `evaluation/independent_eval_harness.py` | 410 | `experimental` | evaluation | Held-out evaluation skeleton: preregistration locks, append-only failure ledger, three-kind recovery counts, scope matrix; no pass-rate computation. U4 journal-quality adjudication is a pending-user-decision hook that always raises. No evaluation runner consumes it yet. | `tests/research_agent/test_eval_harness_track4.py` | - | 2026-11-01 |
| `methods/propensity_weighting.py` | 818 | `experimental` | methods | L2-logistic propensity scores with greedy 1:1 nearest-neighbour matching (caliper) and stabilized IPTW weights plus SMD balance diagnostics. Diagnostics only: the kernel estimates no effect and no Planner/runtime route consumes it. | `tests/research_agent/test_psm_iptw_k7q2m4.py` | - | 2026-11-01 |
| `methods/reclassification.py` | 384 | `experimental` | methods | Categorical/continuous NRI and IDI with bootstrap CIs for binary outcomes. Analysis-only; no Planner/runtime route consumes it. | `tests/research_agent/test_nri_idi_kernel.py` | - | 2026-11-01 |
| `methods/mediation.py` | 423 | `experimental` | methods | Natural direct/indirect effects (linear exact, logistic log-odds approximation). Callers must declare no-interaction and sequential ignorability; violations fail closed. No Planner/runtime route consumes it. | `tests/research_agent/test_mediation_kernel.py` | - | 2026-11-01 |
| `methods/competing_risks.py` | 362 | `experimental` | methods | CIF estimation delegated to lifelines Aalen-Johansen plus descriptive group differences with bootstrap CIs. This CIF kernel has no Gray test or group inference; a separate experimental Gray kernel exists. No Planner/runtime route consumes it. | `tests/research_agent/test_competing_risks_kernel.py` | - | 2026-11-01 |
| `methods/target_trial.py` | 911 | `experimental` | methods | Target-trial scaffolding: eligibility denominator reconciliation, time-zero alignment checks, a clone-censor-weight manual-assumption review sheet, and single-stage dynamic-regime evaluation. Multi-stage regime optimization is absent. No Planner/runtime route consumes it. | `tests/research_agent/test_target_trial_p2skeleton.py` | - | 2026-11-01 |
| `methods/doubly_robust.py` | 440 | `experimental` | methods | AIPTW point estimate with influence-function SE and positivity fail-closed. Separate experimental modules hold sequential MSM weighting, point-treatment g-formula and single-stage dynamic-regime evaluation; this kernel implements none of those. Parked unreachable with reason (offering it evicts survival_inputs from causal landmark steps); un-parking needs prerequisite-aware ranking. | `tests/research_agent/test_doubly_robust_p2skeleton.py` | - | 2026-11-01 |
| `methods/lasso_selection.py` | 367 | `experimental` | methods | Fixed-alpha/CV Lasso selection with pinned KFold splits. Holds a checked-in verified_tool grant (`methods/tool_cards/lasso_variable_selection.card.json`) but no capability route consumes it, so it stays experimental until routed. | `tests/research_agent/test_lasso_shap_cards_ls1.py` | - | 2026-11-01 |
| `methods/shap_attribution.py` | 280 | `experimental` | methods | Exact TreeExplainer attribution for fixed tree models (values only, no plots). Holds a checked-in verified_tool grant (`methods/tool_cards/shap_tree_attribution.card.json`) but no capability route consumes it, so it stays experimental until routed. | `tests/research_agent/test_lasso_shap_cards_ls1.py` | - | 2026-11-01 |
| `methods/msm.py` | 610 | `experimental` | methods | Sequential stabilized IPTW weights over follow-up visits with per-visit diagnostics and positivity fail-closed. Callers must declare sequential exchangeability and correct history encoding; the kernel estimates no effect. Parked unreachable with reason (offering it evicts survival_inputs from causal landmark steps); un-parking needs prerequisite-aware ranking. | `tests/research_agent/test_msm_gformula_x9q4z2.py` | - | 2026-11-01 |
| `methods/gformula.py` | 630 | `experimental` | methods | Point-treatment parametric g-formula average effect with bootstrap CIs. Exchangeability and correct outcome-model specification must be declared; longitudinal g-formula is not built. Parked unreachable with reason (offering it evicts survival_inputs from causal landmark steps); un-parking needs prerequisite-aware ranking. | `tests/research_agent/test_msm_gformula_x9q4z2.py` | - | 2026-11-01 |
| `methods/rcs_dose_response.py` | 867 | `experimental` | methods | Restricted cubic spline dose-response shapes (Harrell knots, Wald nonlinearity test, delta-method curves). Holds a checked-in verified_tool grant (`methods/tool_cards/rcs_dose_response.card.json`). Offered to the Coder for association steps only, pending a no-crowding proof for wider families. | `tests/research_agent/test_rcs_dose_response_rcs1.py` | - | 2026-11-01 |
| `methods/gee.py` | 362 | `experimental` | methods | GEE marginal fits for clustered/longitudinal panels (exchangeable/AR1/independence, robust SEs). Parked unreachable with reason (offering it evicts statsmodels from plain adjusted-logistic steps); clustered steps keep the pooled-fit fallback. | `tests/research_agent/test_gee_mixed_gam_gray_nomogram_v9k4m7.py` | - | 2026-11-01 |
| `methods/mixed_effects.py` | 338 | `experimental` | methods | Linear mixed-model random-intercept fits (minimum five groups, refuses fewer). Parked unreachable with reason (same slot-eviction collision as gee). | `tests/research_agent/test_gee_mixed_gam_gray_nomogram_v9k4m7.py` | - | 2026-11-01 |
| `methods/gam.py` | 488 | `experimental` | methods | Generalized additive fits with B-spline smooth terms and per-term edf. Offered to the Coder for prediction steps. | `tests/research_agent/test_gee_mixed_gam_gray_nomogram_v9k4m7.py` | - | 2026-11-01 |
| `methods/gray_test.py` | 393 | `experimental` | methods | Gray K-sample competing-risk comparison with IPCW adjustment (no R cmprsk oracle; self-proven via log-rank degeneration and hand computation). Offered to the Coder for survival steps. | `tests/research_agent/test_gee_mixed_gam_gray_nomogram_v9k4m7.py` | - | 2026-11-01 |
| `methods/nomogram.py` | 492 | `experimental` | methods | Points-scale nomogram tables from fitted logistic/Cox coefficients (tables only, no figure rendering). Offered to the Coder for prediction steps. | `tests/research_agent/test_gee_mixed_gam_gray_nomogram_v9k4m7.py` | - | 2026-11-01 |

## Rules

1. A new module that no production path calls must be added here in the same
   change that introduces it, with a review date.
2. At the review date the owner picks one of: **make it production-reachable**
   with a typed route and integration test, **keep it experimental** with a new
   reason/date, **disable it**, archive it, or delete it.
3. Do not delete an `experimental` row to make a number look better. The
   engineering rule in `CLAUDE.md` stands: zero production references alone
   never justifies deleting a real implementation — a runner, validator or
   authority needs positive evidence it has been superseded.
4. `methods/` kernels wired without exact typed inputs/outputs and a full data
   contract for `owns_step` are a regression even if tests pass. A
   method-string-to-function mapping is not wiring.
5. A `production_reachable` row without an exact
   `tests/...py::test_name` reachability test is invalid and fails the inventory
   audit. The row must also declare `call:<public entrypoint>` route proof; an
    indirect endpoint additionally declares `trace:<key>` that the test asserts
    after invoking the public entrypoint. Unit tests of the isolated kernel are
    not sufficient.
6. The activation route starts at the first public coordinate the bound test
   actually invokes. Do not prepend an upstream UI or tool route that the cited
   test does not exercise.
