# 运行配置编译后的完整计划接续

开发基线：`9dcef8be4fba0344076fdf57af20ed72ab4ffb98`，原 Dev9 开发分支。修复限于现有计划修订和启动准备 owner，没有增加公共 API、执行器或审批类型。

E2 自主验证4恢复后生成12步候选。Host成功编译了候选的运行配置，但自动重新规划没有携带候选需求，基线从15项缩到仅与主模型3项调整变量有关的内容；`accepted_baseline_requirements`为空。之后 Provider 因额度耗尽返回429，尚未完成修订方案，患者分析0次。环境中断、模型额度失败和需求丢失分别保留；该次自主验证未通过。

## 修复行为

- 启动新的元数据计划时，从已有 `ReviewChoiceProgress` 精确绑定的运行配置编译回执恢复 `PlanChangeRequest`。浏览器不传旧方案、答案或新技术字段。
- 原计划、审阅、检查点、研究配置和上下文哈希均重新校验。只有同一来源/当前版本、且选择记录仅为 Host 的 `typed_runtime_projection`，才在配置摘要变化后保留基线和人群要求。
- 基线表完整内容与主模型调整集分开：前者进入目录、规划检索和完整性检查；后者保留模型已声明的调整变量。真实保存材料回放得到15项基线，主模型仍为3项协变量。
- 既有 `PlanChangeRequirements` 和 `AcceptedBaselineRequirements` 向后传递；旧科学审阅的具体反馈同时进入修订请求。反馈缺失、变化或超预算明确拒绝，不截断后继续调用。
- 来源、问题、版本或用户科学选择变化时，不凭旧运行配置回执继承原范围。既有准备数据、检查点恢复、结果和执行授权路径不扩大；新方案仍需完整审阅。

## 验证

两条新增回归先复现失败：配置编译后的需求为空，以及实际启动准备未恢复修订请求。修复后15文件组合 **203 passed / 5 warnings**，8.73秒；Ruff和diff检查通过。覆盖来源/配置/回执漂移、真实启动准备、零行目录和别名、基线检索、物化、人口范围、稿件基线消费、审阅选择及恢复预算。属于 **combined**，不是 full exact-head CI。

真实保存方案回放不调用外部模型、不读取患者行、不修改原研究文件：故意让模拟选择仅返回乳酸、死亡、年龄、性别和入院类别，实际元数据 owner仍提供全部15项基线；后续规划检索保留全部基线变量。删为3项的候选被既有科学审阅拒绝，完整原基线覆盖通过。修订上下文18,747字符，反馈2,306字符，未通过截断需求降预算。

完整回执在工作区 `task_logs/artifacts/20260910_e2_autonomy_validation_4/terminal/` 和 `repair/`。原失败分母不变。开发九问仍为1/9，E2只算开发辅助完成。离线检查不证明新的分析结果、产品自主完成或发表资格。

## 后续

冻结修复后的源码和一致的运行镜像，再从全新产品会话仅输入原自然问题、来源和交付要求。继续验证自动修订、完整方案审阅、真实分析及报告交付。当前分支还包含其他任务已经复核提交的两批共享修复，其原治理欠账和验证边界见同目录的审阅修复回执，不声称全库CI全绿。
