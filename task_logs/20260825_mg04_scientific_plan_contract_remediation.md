# MG04 scientific-plan contract remediation

## Outcome

Commit `010b3530d399177df58784942bb3eedc19a8ec7c` closes the generic
endpoint, typed sensitivity, descriptive-result figure, and deterministic
balance/association composite-figure planning gaps exposed by the bounded MG04
Planner canary. The branch is pushed as
`origin/codex/literature-to-design-contract`.

This is a **Planner-contract development result**, not a Qualification12
analysis result and not manuscript evidence. No exact execution image was
built, Execute/Coder/Writer were not started, and Held-out27 remains unopened.

## Owner-scoped changes

- Scientific review now accepts a meaningful explicit endpoint description
  instead of rejecting it solely because the internal source-concept field
  retains the generic `declared_primary_outcome` placeholder. Placeholder-only
  descriptions and conflicting endpoint definitions still fail closed.
- `sensitivity_plan_shaping` projects an omitted, user-reviewed typed
  functional-form sensitivity into an explicit analysis-only step. The step
  retains the exact sensitivity-spec ID and literature binding. It does not
  claim a deterministic Method Adapter.
- The locked complete-case robustness replay receives execution credit only
  when the context spec ID, plan robustness spec ID, axis, strategy, and actual
  replay owner agree exactly.
- A deterministic descriptive-context rendering step is bound to a unique
  `table:exposure_outcome_distribution` owner. It displays observed absolute
  risk/prevalence context without changing the primary estimand.
- The association publication renderer now has a deterministic four-table
  profile for balance/positivity context, adjusted association estimates,
  robustness matrix, and robustness summary. It renders only exact upstream
  tables and does not fit or refit a model.

No task ID, MG04, beta-blocker, atrial-fibrillation, Sepsis, or database-specific
branch was added.

## Focused validation

- sensitivity/figure/scientific-review contracts: `79 passed`
- adjacent plan/progressive/cap contracts: `69 passed`
- association sensitivity/publication contracts: `18 passed`
- truthful focused total: `166 passed`, no failures
- targeted Ruff, `py_compile`, and `git diff --check`: pass

The selected long-tail restricted-spline method remains Coder-generated and
`analysis_only`. Only its typed plan obligation is now explicit. The composite
figure renderer is deterministic, but the upstream balance table still belongs
to its declared analysis owner.

## Saved-plan shadow review

The exact saved Planner output from
`/Volumes/外置硬盘/easyicu_data/qualification12_planner_canary_4a135e8_suffix_20260825/MG04/aware/run_20260825T143224_50614e`
was parsed under its saved cohort-concept scope and passed through the current
generic host transforms. The saved final article-figure strategy was reused;
no Provider was called and no plan content was regenerated.

- source plan SHA-256:
  `65d19fbd318a18018e06b5e4753e9ba4ff8032804fcda289c88b42f8e221ca4c`
- transformed shadow-plan SHA-256:
  `90e72e30155bc584bfac360429c8f69249943815a083fd777a7f3e0b8354d9eb`
- plan steps: `15`
- added generic steps:
  `sensitivity_continuous_covariate_functional_form`,
  `15_descriptive_context_figure`
- review: `analysis_only`, approval allowed, score `92`, blockers `0`
- remaining majors:
  `DIRECT_COMPARATOR_NOT_ESTABLISHED`, `NOVELTY_NOT_ESTABLISHED`
- requested and executable sensitivity axes:
  `functional_form`, `missing`
- required figure roles covered:
  `data_quality`, `descriptive_result`, `primary_estimand`, `robustness`
- accepted chart types:
  `availability_panel`, `coverage_heatmap`,
  `dot_interval_absolute_risk`, `forest`, `sensitivity_forest`,
  `specification_grid`

This is explicitly a **shadow review of a saved Planner plan plus current host
transforms**. It is not an official exact-HEAD Planner replay and does not prove
rendered visual quality or execution validity.

## One bounded checkpoint replay

Exactly one zero-Provider checkpoint replay was attempted on exact HEAD
`010b353`. The source checkpoint was
`progressive_planner_checkpoint_011.json`, SHA-256
`26b8fa953c94ab30abc12301ebfccf1a8a8d598ec40658c8f0d18a6579fa460f`.

The run stopped in 3.0 seconds with the intended fail-closed error:

`progressive_resume_outline_authority_mismatch`

The old checkpoint's outline authority differs from the current strict request,
so it cannot be represented as a formal exact-HEAD replay. The run made zero
new Provider calls and was not retried. Failure evidence:

- root:
  `/Volumes/外置硬盘/easyicu_data/qualification12_planner_canary_010b353_checkpoint011_20260825/`
- run:
  `/Volumes/外置硬盘/easyicu_data/qualification12_planner_canary_010b353_checkpoint011_20260825/MG04/aware/run_20260825T145818_4d17b3`
- result:
  `/Volumes/外置硬盘/easyicu_data/qualification12_planner_canary_010b353_checkpoint011_20260825/ehrflowbench_results.json`

## Provider usage boundary

The successful Planner segments that produced the saved 11/11 checkpoint used
9 successful receipts plus one receipt without usage:

- prompt tokens: `87,094`
- completion tokens: `14,818`
- total reported tokens: `101,912`
- conservative cost at `$10/M` input and `$30/M` output: `$1.31548`

The exact-HEAD failed replay and the saved-plan shadow review each used zero
Provider calls, zero reported tokens, and `$0` incremental cost. These counts do
not include earlier unrelated diagnostics recorded in the prior canary log.

## Next gate

Do not call the saved-plan shadow result paper-ready or execution-ready. The
remaining direct-comparator and novelty findings require bounded literature
authority or an explicit not-established conclusion; they should not be closed
with topic-specific code. The efficient next decision is whether to accept this
planner-only development gate and run one fresh bounded Qualification12 item on
a frozen exact image, or first require independent clinical/method review. Do
not start Held-out27 until Qualification12 and the formal freeze gates pass.
