# Idea Mining benchmark inputs

Frozen Idea Mining evaluation rows and screen-seed controls live here.  Runtime
Idea Mining code may load these only for explicit evaluation or development
validation; product execution must not silently depend on benchmark fixtures.
