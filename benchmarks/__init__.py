"""Repository-local benchmark and paper-reproduction packages.

These modules are intentionally outside the installable :mod:`easyicu`
package.  They may consume public research-agent authorities, but the shared
engine must never import a manuscript benchmark.

All benchmark assets live under this package; the former singular
``benchmark/`` tree was retired to avoid two competing owners.
"""
