# Benchmark catalogs

Source-derived, repository-local catalogs used by benchmark and Idea Mining
validation live here.  They are evidence inputs, not installable package data.
Regeneration tools must write to this directory by default.
